# Cron Job: Nightly Skill Health Check

**Job ID:** d93e00debf70
**Run Time:** 2026-04-03 08:07:06
**Schedule:** 0 3 * * *

## Prompt

[SYSTEM: If you have a meaningful status report or findings, send them — that is the whole point of this job. Only respond with exactly "[SILENT]" (nothing else) when there is genuinely nothing new to report. [SILENT] suppresses delivery to the user. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

定期检查 ~/.hermes/skills/ 目录下所有 skill 的健康状态。

具体执行：
1. 用 find ~/.hermes/skills/ -name "SKILL.md" 找到所有 SKILL.md 文件
2. 每个 SKILL.md 的父目录就是一个 skill（例如 ~/.hermes/skills/apple/apple-reminders/SKILL.md → skill 名 apple/apple-reminders）
3. 对每个 skill 检查：
   a. SKILL.md 是否存在
   b. frontmatter 是否包含 name、description、triggers、steps
   c. 读取 steps 里的文件引用（如 references/、scripts/、templates/ 下的文件），检查是否都存在
4. 输出格式：
   - 正常：[✓] skill-name
   - 异常：[✗] skill-name — 问题描述
   - 汇总：共检查 N 个，正常 M 个，异常 K 个

如果全部正常，输出"所有 skills 检查通过"。
如果有任何异常，列出详情但不要尝试自动修复（只报告，不改动）。

注意：skills 目录结构是嵌套的，不要只扫顶级目录名。

## Response

共检查 111 个，正常 101 个，异常 10 个

---

**[✗] autonomous-ai-agents/hermes-agent** — 引用文件不存在: `scripts/install.sh`

**[✗] data-science/jupyter-live-kernel** — 引用文件不存在: `scripts/jupyter_live_kernel.py`

**[✗] github/github-code-review** — 引用文件不存在: `scripts/gh-env.sh`

**[✗] mlops/models/llava** — 引用文件不存在: `scripts/v1_5/pretrain.sh`; `scripts/v1_5/finetune.sh`

**[✗] mlops/models/segment-anything** — 引用文件不存在: `scripts/export_onnx_model.py`

**[✗] mlops/training/flash-attention** — 引用文件不存在: `references/algorithm.md`（重复）; `references/advanced-features.md`（重复）

**[✗] mlops/training/simpo** — 引用文件不存在: `scripts/run_simpo.py`（重复）

**[✗] mlops/training/slime** — 引用文件不存在: `scripts/models/qwen3-4B.sh`; `scripts/models/`（路径不完整）

**[✗] mlops/training/torchtitan** — 引用文件不存在: `scripts/download_hf_assets.py`（重复）

**[✗] productivity/powerpoint** — 引用文件不存在: `scripts/thumbnail.py`; `scripts/office/unpack.py`; `scripts/office/soffice.py`（重复）

---

均为**文件缺失**问题，SKILL.md 和 frontmatter 均正常。所有引用路径均为 `scripts/` 或 `references/` 下的文件，疑为 skill 安装不完整或存根占位。
